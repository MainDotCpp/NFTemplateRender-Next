'use client'
import { observer } from 'mobx-react'

const TemplateRenderEngine = () => {
  return (
    <div>
      <div id="container"></div>
    </div>
  )
}

export default observer(TemplateRenderEngine)
